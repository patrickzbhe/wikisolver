import React from "react";

import './Navbar.css'

const Navbar = ({ location }) => {
    return (
        <div className="topnav">
            <a href="/">Home</a>
        </div>
    );
}

export default Navbar;